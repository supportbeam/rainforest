<!DOCTYPE html>
<html>
  <head>
    <title>Rainforest</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
<!-- display notices or alerts whenever available. The application view is appropriate because we only have to write this code once and it can be reused across all views. -->
  	<% if flash[:notice] %>
      <p id="notice"><%= flash[:notice] %></p>
  	<% end %>
  	<% if flash[:alert] %>
      <p id="alert"><%= flash[:alert] %></p>
  	<% end %>

<!-- yield to code from all other html files from the app -->
  	<%= yield %> 
    
    <!-- At the bottom of the page, if the user is logged in we want to display "Signed in as [USERNAME]" or else display "Login or Sign up" -->
    <p class="userinfo">
      <% if current_user %>
        Signed in as <%= current_user.email %>. <%= link_to "Log out", sessions_path("current"), :method => :delete %>
      <% else %>
        <%= link_to "Log in", new_sessions_path %> or <%= link_to "Sign up", new_user_path %>
      <% end %>
    </p>
  </body>
</html>
