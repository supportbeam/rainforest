<!DOCTYPE html>
<html>
  <head>
    <title>Rainforest</title>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body>
<!-- display notices or alerts whenever available. The application view is appropriate because we only have to write this code once and it can be reused across all views. 
If a notice key in the flash hash exists, display the value. If an alert key in the flash hash exists, display the value.
-->
  	<% if flash[:notice] %>
      <p id="notice"><%= flash[:notice] %></p>
  	<% end %>
  	<% if flash[:alert] %>
      <p id="alert"><%= flash[:alert] %></p>
  	<% end %>

    <nav>
      <%= link_to "Home", root_path %>
      <% if current_user %>
        Signed in as <%= current_user.name %> | <%= link_to "Log out", sessions_path("current"), :method => :delete %>
      <% else %>
        <%= link_to "Log in", new_sessions_path %> or <%= link_to "Sign up", new_user_path %>
      <% end %>
    </nav>
    
    <!-- yield to code from all other html files from the app -->
    <%= yield %> 

  </body>
  
</html>
